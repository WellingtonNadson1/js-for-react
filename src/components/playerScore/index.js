import './style.css'

function PlayerScore(player1, player2){

    return `
        <section>
        
        </section>
        <h2>
            ${player}
        </h2>
    `
};

export default PlayerScore;